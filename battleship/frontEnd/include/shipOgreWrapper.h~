#ifndef _SHIP_OGRE_WRAPPER_H
#define _SHIP_OGRE_WRAPPER_H
#include <Ogre.h>
#include "ship.h"
class ShipOgreWrapper {
 private:
  char* _id;
  //ship* _ship;

 public:
  ShipOgreWrapper(char* id);
  ~ShipOgreWrapper();
  void paintShip(Ogre::SceneManager * _sceneManager,
                             Ogre::SceneNode * _parentNode,                             
                              Ogre::Vector3* position);
  
};





#endif
